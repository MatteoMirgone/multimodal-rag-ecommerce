# Core ML / vision-language
torch>=2.0
torchvision>=0.15
transformers>=4.36,<5      # MUST stay below 5.0 — transformers 5.x breaks CLIP's
                           # get_text_features / get_image_features (returns an
                           # object instead of a tensor). 4.x is required.
pillow>=10.0

# Data
pandas>=2.0
pyarrow>=14.0
numpy>=1.24

# Vector database
chromadb>=0.4.22           # After running the Colab notebook, change this to the
                           # exact version it prints, e.g.  chromadb==1.0.0
                           # so the app reads the index with the same version that built it.

# LLM clients (install the one you use)
groq>=0.4.0
# together>=1.2.0    # uncomment if using Together AI

# UI
streamlit>=1.30

# Misc
requests>=2.31
tqdm>=4.66
matplotlib>=3.7
scikit-learn>=1.3
# umap-learn>=0.5   # optional: for embedding visualization (heavy — left off by default)
